# 📊 BlueGuard 데이터 수집 계획

---

**🎯 목표:** 해수욕장 수질 데이터와 관련 기상 데이터를 확보하여 분석 가능한 형태로 준비한다.

---

* :link: 공모전 경로 : [mof.go.kr](https://www.mof.go.kr/contest#0)


## 1. 데이터 소스 파악 및 접근 방법

*   **🌊 해수욕장 수질 데이터:**
    *   **주요 소스:** 
        *   해양환경정보포털 (해양수산부 운영)
        *   환경부 물환경정보시스템
        *   공공데이터포털 ([data.go.kr](http://data.go.kr))
    *   **검색 키워드:** `해수욕장 수질`, `연안 수질`, `해수 수질측정망` 등
    *   **수집 방법:**
        *   **OpenAPI 우선 검토:** `Python` (`requests` 라이브러리) 활용하여 API 호출 자동화 (API Key 발급 필요)
        *   **차선책 (파일 다운로드):** 해당 웹사이트에서 직접 `CSV` 또는 `Excel` 파일 다운로드 (업데이트 주기 확인)

*   **☀️ 기상 데이터:**
    *   **주요 소스:** 기상청 기상자료개방포털 ([data.kma.go.kr](http://data.kma.go.kr))
    *   **검색 키워드:** `종관기상관측(ASOS)`, `방재기상관측(AWS)`, `해양기상 부이` 등 (해수욕장 인근 관측소 데이터 활용)
    *   **수집 방법:**
        *   **OpenAPI 우선 활용:** `Python` (`requests` 라이브러리) 활용하여 API 호출 (API Key 발급 필요)
        *   **차선책 (파일 다운로드):** 필요시 파일 다운로드 옵션 확인

---

## 2. 필요한 데이터 필드 정의

*   **💧 수질 데이터 필드:**
    *   `측정일시`: 년, 월, 일, 시간 (최대한 상세하게)
    *   `해수욕장 명칭` 또는 `ID`: 고유 식별자
    *   `측정 지점 위도/경도`: 지도 시각화 및 기상 데이터 매칭용
    *   `대장균 수`: Colony Forming Unit/100mL
    *   `장구균 수`: Colony Forming Unit/100mL
    *   *(선택/가능시)* `부유물질(SS)`, `수온`, `염분`, `pH`, `용존산소(DO)` 등

*   **🌡️ 기상 데이터 필드:**
    *   `관측일시`: 년, 월, 일, 시간
    *   `관측 지점 명칭` 또는 `ID`
    *   `관측 지점 위도/경도`
    *   `강수량` (mm)
    *   `기온` (°C)
    *   `수온` (°C - 해양 부이 등에서 확보 가능 시)
    *   `풍향` (16방위 또는 360각도)
    *   `풍속` (m/s)
    *   `현지기압` 또는 `해면기압` (hPa)
    *   *(선택/가능시)* `습도`(%), `일사량`(MJ/m²), `파고`(m) 등

---

## 3. 데이터 수집 및 초기 가공 계획

*   **수집 기간:** 분석 목표에 따라 설정 (예: 최근 3~5년치)
*   **수집 주기:** 데이터 업데이트 주기에 맞춰 설정 (예: 일별, 주별)
*   **데이터 저장:**
    *   수집된 원본 데이터: `JSON` 또는 `CSV` 형태로 저장
    *   가공된 데이터: `Pandas DataFrame`으로 관리 후 `CSV` 또는 `pickle` 형태로 저장
*   **초기 가공:**
    1.  **데이터 병합:** 수질 & 기상 데이터를 `위치`(위도/경도 기반 근접) 및 `일시` 기준으로 병합
    2.  **형식 통일:** 날짜/시간 형식 일관성 확보
    3.  **결측치 처리:** 확인 및 처리 방안 결정 (제거, 평균/중앙값/최빈값 대체, 예측 모델 기반 대체 등)
    4.  **단위 통일:** 필요한 경우 단위 변환
    5.  **타입 변환:** 숫자형(numeric), 범주형(categorical) 등으로 데이터 타입 정리

---

## 4. 역할 분담

*   **👤 담당자 A (김재협):**
    *   해수욕장 수질 데이터 소스 확정 (API or 파일)
    *   수질 데이터 수집 자동화 스크립트 개발 또는 파일 다운로드/정리
    *   수질 데이터 초기 전처리 (결측치, 단위 등)

*   **👤 담당자 B (김동근):**
    *   기상 데이터 소스 확정 (API 위주)
    *   기상 데이터 수집 자동화 스크립트 개발
    *   기상 데이터 초기 전처리
    *   수질-기상 데이터 병합 로직 개발 및 실행

---

## ➡️ 다음 단계

1.  각 담당자는 맡은 데이터 소스의 **API 명세서/다운로드 페이지 확인** (URL, 요청 파라미터, 응답 형식 등).
2.  필요한 **API 활용 신청 및 인증키 발급** (공공데이터포털, 기상자료개방포털 등).
3.  간단한 `Python` 스크립트로 **샘플 데이터 수집 테스트** 및 필드 확인. 
